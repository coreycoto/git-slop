# Agent Decision Patterns

Useful patterns for project-management workflows:

- preview first, apply second
- deterministic checked-in config before live mutation
- quarter commitments as milestone deltas, not roadmap buckets
- sparse labels over taxonomic sprawl
- repo-local artifacts as the durable evidence layer
