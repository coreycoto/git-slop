interface:
  display_name: "Quarter Plan Apply"
  short_description: "Apply reviewed quarter commitments and record rationale"
  default_prompt: "Use $plan-quarter-apply to apply reviewed quarter commitments and record the rationale in GitHub."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "connector"
      value: "github"
      description: "Official GitHub Codex plugin"
