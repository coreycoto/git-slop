interface:
  display_name: "Label Palette Design"
  short_description: "Preview or refresh the deterministic backlog label palette"
  default_prompt: "Use $label-palette-design to preview or refresh the deterministic GitHub label palette."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "connector"
      value: "github"
      description: "Official GitHub Codex plugin"
