interface:
  display_name: "Install Git Slop"
  short_description: "Install and verify git-slop"
  default_prompt: "Use $git-slop:install-update to install or update a published git-slop CLI and verify its version and source revision."

policy:
  allow_implicit_invocation: true
