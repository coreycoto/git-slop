interface:
  display_name: "Adopt Git Slop"
  short_description: "Adopt git-slop locally and in CI"
  default_prompt: "Use $git-slop:adopt-repo to wire a repository to the crates.io-backed native CLI and advisory GitHub Action distribution contract."

policy:
  allow_implicit_invocation: true
