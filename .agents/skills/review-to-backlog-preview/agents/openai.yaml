interface:
  display_name: "Review Backlog Preview"
  short_description: "Preview backlog-ready issues from repo review findings"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  brand_color: "#4338CA"
  default_prompt: "Use $review-to-backlog-preview to review the current git-slop repository and preview the backlog issues that material findings should become."

policy:
  allow_implicit_invocation: true
