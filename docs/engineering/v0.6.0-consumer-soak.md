# `v0.6.0` Consumer Soak

This note records the first consumer soak after shipping `git-slop v0.6.0`.

## Repos exercised

- `git-slop`
  - `find`
  - `explain --path src/git_slop/organization.py`
  - `explain --path src/git_slop`
  - `plan --relationship duplicate_neighborhood-0118297c698a`
  - `plan --cluster scattered_concept-5e8b735be418`
  - `explain --top 5`
- `mature validation repo`
  - `./scripts/git_slop.sh find`
  - `explain --path src/consumer_app/commands/handlers/discover.py`
  - `explain --path src/consumer_app`
  - `plan --relationship near_duplicate_neighborhood-f7e46acfd0f8`
  - `plan --cluster scattered_concept-5e0509e47d45`
- `consumer toolkit repo`
  - fresh local `find`
  - `explain --path src/consumer_toolkit/skills/metadata.py`
  - `plan --relationship near_duplicate_neighborhood-35e7fad1c4e0`
  - `plan --cluster scattered_concept-89f3da03e6ed`

## Consumer alignment

- the mature validation repo was updated locally from `git-slop @ v0.5.2` to `git-slop @ v0.6.0`
- the lockfile refreshed cleanly
- the existing packaging contract test stayed green on the new pin

## Outcome

- hotspot explanations still read as context cost first
- overlays stayed additive evidence rather than verdicts
- `plan` slices stayed bounded and reviewable across all three repos
- the consumer toolkit repo still suppresses the prior spill-heavy relationship follow-up
- the mature validation repo stayed clean on the `v0.6.0` pin during the consumer pass

## Repo-local accepted noise

- `git-slop`
  - the deterministic cluster selector landed on a fixture-heavy structural cluster
  - the output still stayed bounded, so this remains accepted repo-local noise rather than a shipped regression
- `mature validation repo`
  - broad fixture-heavy structural clusters remain expected in a mature mixed code-and-fixture repo
  - the selected broad cluster still started from a tight relationship-backed sub-slice

## Contract confirmation

The consumer soak did **not** justify:

- detector-score changes
- `check` changes
- report schema changes
- config schema changes
- a `git-slop v0.6.1` regression release

`v0.6.0` is accepted as the baseline for the maintainer-facing `plan`-to-backlog preview wave.
