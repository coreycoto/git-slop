# `v0.4.0` Compatibility Notes

Date: 2026-04-22

This note records the first consumer compatibility pass after releasing
`git-slop v0.4.0`.

## Consumers checked

### `deeptravel`

- Result: compatible after a repo-local dependency bump only.
- Required repo-local change:
  - update the dev dependency from `git-slop @ ...@v0.1.1` to `git-slop @ ...@v0.4.0`
  - refresh `uv.lock`
- Existing entrypoint stayed unchanged:
  - `./scripts/git_slop.sh find`
  - `./scripts/git_slop.sh show ... --format json`
- Verified after the bump:
  - `.slop/latest/report.json`, `report.yaml`, and `summary.md` generate cleanly
  - report contract is now `schema_version: 3`
  - nested `costs` and `overlays` are present
  - compatibility mirrors remain present for the compatibility window

### `neuroscribes`

- Result: compatible with no code changes.
- Existing repo-local `.slop/config.yaml` remained accepted.
- Verified with `git-slop v0.4.0`:
  - `.slop/latest/report.json`, `report.yaml`, and `summary.md` generate cleanly
  - report contract is `schema_version: 3`
  - nested `costs` and `overlays` are present
  - compatibility mirrors remain present for the compatibility window

## Outcome

No detector-level compatibility follow-up was opened from this pass.

Reasoning:

- the only break was consumer-local pinning in `deeptravel`
- `neuroscribes` worked against the released detector without code changes
- no shared detector bug appeared across both consumers
